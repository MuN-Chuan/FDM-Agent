{
  "name": "@fdm-ai/client-agent",
  "version": "1.0.0",
  "description": "FDM-AI 本地 Client Agent — 通过 WebSocket 调用本机 BambuStudio CLI 执行 3MF 导出",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test test/**/*.test.js",
    "test:watch": "node --test --watch test/**/*.test.js"
  },
  "keywords": [
    "fdm",
    "bambu",
    "3mf",
    "client-agent"
  ],
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "form-data": "^4.0.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
