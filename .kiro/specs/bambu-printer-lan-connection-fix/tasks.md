# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - LAN Connection Failure with Cloud-Provided IP
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: Scope the property to concrete failing cases - cloud MQTT returns stale/incorrect IP, but printer is reachable on LAN
  - Test implementation details from Bug Condition in design:
    - Mock cloud MQTT to return IP address (e.g., "192.168.1.100" or wrong subnet "10.0.0.50")
    - Mock printer as reachable on LAN at different IP (e.g., "192.168.1.200")
    - Call `collectPrinterStatuses` with machine having cloud-provided IP
    - Assert that connections fail: `mqtt=false`, `ftp=false`, `lan_online=false`
  - The test assertions should match the Expected Behavior Properties from design:
    - After fix: `mqtt=true`, `ftp=true`, `lan_online=true` with valid reachable IP
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found to understand root cause:
    - Which IP addresses fail (stale, wrong subnet, etc.)
    - Whether Bambu Studio cache has better IPs
    - Whether timeouts are insufficient
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Offline and Cloud-Only Detection
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs:
    - Offline printers: observe `lan_online=false`, `mqtt=false`, `ftp=false`
    - Cloud-only printers (not on LAN): observe `cloud_online=true`, `lan_online=false`
    - Working LAN connections: observe successful connections with correct IPs
    - Multi-printer scenarios: observe independent status checking
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - Property: For all offline printers, status shows `lan_online=false`
    - Property: For all cloud-only printers, status shows `cloud_online=true` and `lan_online=false`
    - Property: For all printers with correct IPs, LAN connections succeed
    - Property: For all multi-printer configs, each printer checked independently
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. Fix for LAN connection failure with cloud-provided IP addresses

  - [x] 3.1 Enhance IP scoring and selection logic
    - Modify `shouldReplaceMachineIp` function to consider subnet matching
    - Add subnet scoring using existing `scoreIpCandidate` function
    - Prefer same-subnet IPs (score 100) over different-subnet private IPs (score 10)
    - Only replace existing same-subnet IPs with other same-subnet IPs
    - _Bug_Condition: isBugCondition(input) where input.machine.ip from cloud_mqtt fails LAN connection but printer is reachable_
    - _Expected_Behavior: System discovers and uses valid, reachable LAN IP resulting in mqtt=true, ftp=true, lan_online=true_
    - _Preservation: Offline detection (lan_online=false for offline printers), cloud-only connectivity (cloud_online=true with lan_online=false), working LAN connections remain unchanged_
    - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2, 3.3_

  - [x] 3.2 Add IP validation before use
    - Create new `validateIpReachability` function
    - Perform quick connectivity check (ping or TCP connect with short timeout)
    - Return boolean indicating if IP is likely reachable
    - Use this validation before attempting full MQTT/FTP connections
    - _Bug_Condition: isBugCondition(input) where cloud IP is not validated before use_
    - _Expected_Behavior: System validates IP reachability before attempting connections_
    - _Preservation: Offline detection and cloud-only connectivity remain unchanged_
    - _Requirements: 2.1, 2.2, 2.4, 3.1_

  - [x] 3.3 Implement fallback IP discovery
    - Modify `collectPrinterStatuses` to retry with alternative IPs when connections fail
    - Check Bambu Studio cache for alternative IPs after connection failure
    - Try same-subnet IPs from cache before giving up
    - Update machine IP if alternative succeeds
    - _Bug_Condition: isBugCondition(input) where cloud IP fails but cache has valid IP_
    - _Expected_Behavior: System falls back to cache IPs and successfully connects_
    - _Preservation: Existing cache enrichment via enrichMachineIpsFromBambuStudioCache continues to function_
    - _Requirements: 2.1, 2.2, 2.4, 3.5_

  - [x] 3.4 Prioritize Bambu Studio cache for same-subnet IPs
    - Enhance `enrichMachineIpsFromBambuStudioCache` to replace cloud IPs when cache has higher-scored IPs
    - Allow cache to replace cloud IPs when cache has same-subnet IPs (score 100)
    - Run cache enrichment after cloud enrichment but before status collection
    - Add logic to prefer cache IPs with score 100 over cloud IPs with lower scores
    - _Bug_Condition: isBugCondition(input) where cache has better IP than cloud_
    - _Expected_Behavior: System prioritizes same-subnet cache IPs over cloud IPs_
    - _Preservation: Existing cache scanning and enrichment logic remains unchanged_
    - _Requirements: 2.1, 2.4, 3.5_

  - [x] 3.5 Adjust timeout values for more reliable connections
    - Consider increasing `MQTT_TIMEOUT_MS` from 7000 to 10000 (10 seconds)
    - Consider increasing `FTP_TIMEOUT_MS` from 5000 to 7000 (7 seconds)
    - Ensure `DISCOVERY_TIMEOUT_MS` remains sufficient at 9000ms
    - Test with various network conditions to validate timeout values
    - _Bug_Condition: isBugCondition(input) where timeouts are too short for network conditions_
    - _Expected_Behavior: System allows sufficient time for connections to establish_
    - _Preservation: Offline detection timing remains appropriate_
    - _Requirements: 2.2, 2.5, 3.1_

  - [x] 3.6 Add logging for debugging
    - Insert debug logging to track IP selection and connection attempts
    - Log which IP source is being used (cloud_mqtt vs cache)
    - Log IP scores and subnet matching results
    - Log connection failure reasons for troubleshooting
    - _Bug_Condition: N/A (debugging aid)_
    - _Expected_Behavior: System provides visibility into IP selection and connection process_
    - _Preservation: No impact on existing behavior_
    - _Requirements: 2.1, 2.2, 2.4_

  - [x] 3.7 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - LAN Connection Success with Valid IP
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify that for inputs where bug condition holds, system now:
      - Discovers valid, reachable LAN IP
      - Successfully establishes MQTT and FTP connections
      - Reports `lan_online=true`, `mqtt=true`, `ftp=true`
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.8 Verify preservation tests still pass
    - **Property 2: Preservation** - Offline and Cloud-Only Detection
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all preservation properties still hold:
      - Offline printers still show `lan_online=false`
      - Cloud-only printers still show `cloud_online=true`, `lan_online=false`
      - Working LAN connections still succeed
      - Multi-printer independence preserved
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise
  - Verify bug condition test passes (LAN connections succeed with valid IPs)
  - Verify preservation tests pass (offline detection, cloud-only, working connections unchanged)
  - Verify unit tests pass (IP scoring, validation, cache prioritization)
  - Verify integration tests pass (full login flow, status collection, fallback logic)
